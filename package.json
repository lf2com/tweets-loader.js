{
  "name": "tweets-loader.js",
  "version": "1.0.0",
  "description": "",
  "main": "tweets-loader.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build-debug": "browserify tweets-loader.js -t [ babelify --presets [ es2015 ] ] > tweets-loader.min.js",
    "build": "browserify tweets-loader.js -t [ babelify --presets [ es2015 ] ] | uglifyjs -cm > tweets-loader.min.js"
  },
  "author": "Wan Wan",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^8.0.0",
    "browserify": "^15.0.0",
    "uglify-js": "^3.3.5"
  }
}
